{
  "name": "@rprend/alpha-fixture",
  "version": "0.1.0",
  "type": "module",
  "description": "Fixture datasets for Alpha Research.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "package.json"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@rprend/alpha-core": "0.1.0"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "test": "node --import tsx --test test/*.test.ts"
  }
}
