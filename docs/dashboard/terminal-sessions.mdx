---
title: "Terminal Sessions"
description: "Review persisted RESEARCH CLI sessions proxied through Alpha Research."
---

Terminal sessions are persisted CLI orchestration sessions visible in the dashboard.

Use them when you need to:

- reconnect a browser view to work started from the CLI
- inspect what the CLI asked the backend to do
- recover context after an async run starts
- distinguish terminal progress from final run artifacts

