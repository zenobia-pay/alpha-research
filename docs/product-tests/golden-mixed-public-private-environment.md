# golden: mixed public private environment

## Product Use

A user asks to set up an economics research environment using FRED plus a private sales export.

## Actions Taken

The product checks existing datasets, creates a mixed research environment, and gives the user the build run id and dashboard link.

## Assertions Made

- Existing datasets are listed first.
- A research environment build is started.
- Public and private source planning is represented.
- The response includes `run-env-1`.
- The response includes the canonical dashboard link.
